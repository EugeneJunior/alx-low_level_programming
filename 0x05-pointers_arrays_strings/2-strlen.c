#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: pointer to the string
 *
 * Return: returns length of string
 */

int _strlen(char *s)
{


	int a =  (int) strlen(s);

	return (a);

}
