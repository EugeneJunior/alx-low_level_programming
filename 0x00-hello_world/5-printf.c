#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 *     */
int main(void)
{
	char hello[] = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n", hello);
	return (0);
}
